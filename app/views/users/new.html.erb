<div class="container">
    <div class="row">
        <div class="col">
            <div class="new-form">
                <h1>New User</h1>
                <%= render 'form', user: @user %>
                <%= link_to 'Back', user_path(@current_user.id) %>
            </div>
        </div>
    </div>
</div>
